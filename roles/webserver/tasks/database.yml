---
- name: Install pip
  yum: name=python-pip state=present

- name: Create database user
  mysql_user:
      name: root
      password: F@1zur@@studyroom2308000
      priv: "database.*:ALL"
      state: present
      login_unix_socket: /var/lib/mysql/mysql.sock

- name: Create Database
  mysql_db: db=splti state=present